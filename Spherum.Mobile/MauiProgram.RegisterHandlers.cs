namespace Spherum.Mobile;

public static partial class MauiProgram
{
    // check platforms folder for platform specific implementation
    static partial void RegisterHandlers(this IMauiHandlersCollection handlers);
}